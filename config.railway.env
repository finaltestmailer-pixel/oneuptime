# Railway Production Configuration
ENVIRONMENT=production
NODE_ENV=production
HOST=${{RAILWAY_PUBLIC_DOMAIN}}
PROVISION_SSL=true
ONEUPTIME_HTTP_PORT=3002
HTTP_PROTOCOL=https
CAPTCHA_ENABLED=false

# Security - Railway will inject these
ONEUPTIME_SECRET=${{ONEUPTIME_SECRET}}
REGISTER_PROBE_KEY=${{REGISTER_PROBE_KEY}}
DATABASE_PASSWORD=${{DATABASE_PASSWORD}}
CLICKHOUSE_PASSWORD=${{CLICKHOUSE_PASSWORD}}
REDIS_PASSWORD=${{REDIS_PASSWORD}}
ENCRYPTION_SECRET=${{ENCRYPTION_SECRET}}

# Database Configuration (Railway PostgreSQL)
DATABASE_HOST=${{PGHOST}}
DATABASE_PORT=${{PGPORT}}
DATABASE_USERNAME=${{PGUSER}}
DATABASE_PASSWORD=${{PGPASSWORD}}
DATABASE_NAME=${{PGDATABASE}}
DATABASE_SSL_REJECT_UNAUTHORIZED=false

# Redis Configuration (Railway Redis)
REDIS_HOST=${{REDIS_HOST}}
REDIS_PORT=${{REDIS_PORT}}
REDIS_PASSWORD=${{REDIS_PASSWORD}}
REDIS_USERNAME=default

# ClickHouse Configuration (Disabled for Railway - not available)
# CLICKHOUSE_USER=${{CLICKHOUSE_USER}}
# CLICKHOUSE_PASSWORD=${{CLICKHOUSE_PASSWORD}}
# CLICKHOUSE_DATABASE=${{CLICKHOUSE_DATABASE}}
# CLICKHOUSE_HOST=${{CLICKHOUSE_HOST}}
# CLICKHOUSE_PORT=8123

# Disable ClickHouse-dependent features
DISABLE_CLICKHOUSE=true

# App Configuration
PORT=3002
APP_PORT=3002
TELEMETRY_PORT=3403
WORKER_PORT=1445
HOME_PORT=1444

# Fix internal API calls - use localhost instead of 'api'
SERVER_APP_HOSTNAME=localhost
SERVER_TELEMETRY_HOSTNAME=localhost
SERVER_WORKER_HOSTNAME=localhost
TELEMETRY_HOSTNAME=localhost:3403

# Disable complex features for Railway
BILLING_ENABLED=false
IS_ENTERPRISE_EDITION=false
DISABLE_AUTOMATIC_INCIDENT_CREATION=false
DISABLE_AUTOMATIC_ALERT_CREATION=false

# Logging
LOG_LEVEL=INFO

# Probe Configuration
GLOBAL_PROBE_1_NAME="Railway-Probe-1"
GLOBAL_PROBE_1_DESCRIPTION="Railway deployment probe"
GLOBAL_PROBE_1_MONITORING_WORKERS=2
GLOBAL_PROBE_1_MONITOR_FETCH_LIMIT=5
GLOBAL_PROBE_1_ONEUPTIME_URL=https://${{RAILWAY_PUBLIC_DOMAIN}}
GLOBAL_PROBE_1_PORT=3874
GLOBAL_PROBE_1_KEY=${{GLOBAL_PROBE_1_KEY}}

# Disable telemetry for Railway
DISABLE_TELEMETRY_FOR_ACCOUNTS=true
DISABLE_TELEMETRY_FOR_APP=true
DISABLE_TELEMETRY_FOR_TELEMETRY=true
DISABLE_TELEMETRY_FOR_WORKER=true

# Additional required variables for startup
APP_TAG=release
COMPOSE_PROJECT_NAME=oneuptime
REDIS_DB=0
REDIS_IP_FAMILY=
REDIS_TLS_CA=
REDIS_TLS_SENTINEL_MODE=false
DATABASE_SSL_CA=
DATABASE_SSL_KEY=
DATABASE_SSL_CERT=
TEST_SERVER_PORT=3800
ANALYTICS_KEY=
ANALYTICS_HOST=
WORKFLOW_SCRIPT_TIMEOUT_IN_MS=5000
WORKFLOW_TIMEOUT_IN_MS=5000
TELEMETRY_CONCURRENCY=100
FLUENT_LOGS_CONCURRENCY=100
WORKER_CONCURRENCY=100
ALLOWED_ACTIVE_MONITOR_COUNT_IN_FREE_PLAN=10